+++
title = 'Books'
description = 'Textbooks, with problem sets and solutions.'
weight = 10

# Keeps this entry point out of the main sidebar tree everywhere except while
# actually browsing inside a book — where its own chapters render normally.
# Reached instead via the "Books" shortcut at the bottom of the sidebar (see
# config/_default/menus.toml).
[params]
  hidden = true
+++

{{< book-gallery >}}
